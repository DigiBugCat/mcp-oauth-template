# GitHub OAuth App Credentials
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# OAuth Provider Configuration
COOKIE_ENCRYPTION_KEY=your_random_encryption_key_at_least_32_chars

# MCP Server Configuration
MCP_SERVER_URL=http://host.docker.internal:8080

# Environment
ENVIRONMENT=development
LOG_LEVEL=DEBUG

# IMPORTANT: Set this to your ngrok URL when testing
PUBLIC_URL=https://your-subdomain.ngrok-free.app

# Optional: Access Control (comma-separated lists)
# ALLOWED_GITHUB_USERS=user1,user2
# ALLOWED_GITHUB_ORGS=org1,org2
# ALLOWED_GITHUB_TEAMS=org1/team1,org2/team2
# ALLOWED_EMAIL_DOMAINS=company.com,example.org

# Pre-configured OAuth clients
# Format: JSON array of client configurations
# Example includes Claude Desktop (public client) configuration
PRECONFIGURED_OAUTH_CLIENTS=[{"client_id":"claude-desktop-client","client_secret":"","redirect_uris":["http://localhost:51009/callback"],"client_name":"Claude Desktop","grant_types":["authorization_code","refresh_token"],"response_types":["code"],"scope":"mcp","token_endpoint_auth_method":"none"}]

# Optional: Security settings
# DISABLE_RATE_LIMITING=true
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
# ALLOWED_ORIGINS=https://app.yourdomain.com